# 지도 삽입 정책 변경

## 📋 변경 사항

### Before (이전)
- ❌ 방문체험단/기자단: 지도 삽입 **필수** (체크박스 비활성화)
- ❌ 유효성 검사: 지도 삽입 체크 안 되면 폼 제출 불가
- ❌ 웹서비스 기자단 등 예외 케이스 처리 불가

### After (현재)
- ✅ 방문체험단/기자단: 지도 삽입 **권장** (체크박스 활성화)
- ✅ 유효성 검사: 지도 삽입 여부와 무관하게 폼 제출 가능
- ✅ 웹서비스 기자단 등 예외 케이스 처리 가능

---

## 🎯 정책 상세

### 1. 기본값
- **방문체험단/기자단**: 지도 삽입 체크박스 **기본값 true**
- **배송체험단**: 지도 삽입 옵션 표시 안 함

### 2. 선택 가능
- ✅ 체크박스 활성화
- ✅ 사용자가 자유롭게 체크/해제 가능
- ✅ 웹서비스 기자단, 온라인 이벤트 등에서 해제 가능

### 3. 안내 메시지
```
💡 방문체험단/기자단은 지도 삽입을 권장합니다. 
   (웹서비스 기자단 등은 제외 가능)
```

- **색상**: 파란색 (정보성)
- **톤**: 권장 사항 (필수 아님)

---

## 📊 사용 시나리오

### 시나리오 1: 일반 방문체험단 (카페, 레스토랑 등)
```
✅ 지도 삽입: 체크됨 (기본값)
→ 리뷰어가 블로그에 네이버 지도 삽입
```

### 시나리오 2: 웹서비스 기자단
```
☑️ 지도 삽입: 해제
→ 웹서비스는 물리적 위치가 없으므로 지도 불필요
```

### 시나리오 3: 온라인 이벤트 기자단
```
☑️ 지도 삽입: 해제
→ 온라인 이벤트는 지도 삽입 불필요
```

---

## 🔧 기술 구현

### 1. 유효성 검사 제거
```typescript
// Before (제거됨)
if ((step1Data.campaignType === 'visit' || step1Data.campaignType === 'press') 
    && step1Data.includeNaver) {
    if (!formData.blogMapRequired) {
        return false;  // ❌ 지도 삽입 필수
    }
}

// After (현재)
// 지도 삽입 유효성 검사 없음 ✅
```

### 2. UI 변경
```tsx
// Before
<input
    type="checkbox"
    checked={formData.blogMapRequired}
    disabled={true}  // ❌ 비활성화
/>
<span>지도 삽입 필수 <span className="text-red-500">*</span></span>

// After
<input
    type="checkbox"
    checked={formData.blogMapRequired}
    // disabled 제거 ✅ 활성화
/>
<span>지도 삽입</span>  // 필수 표시 제거
```

### 3. 안내 메시지 변경
```tsx
// Before
<p className="text-amber-700 bg-amber-50">
    ℹ️ 방문체험단/기자단은 지도 삽입이 필수입니다.
</p>

// After
<p className="text-blue-700 bg-blue-50">
    💡 방문체험단/기자단은 지도 삽입을 권장합니다. 
       (웹서비스 기자단 등은 제외 가능)
</p>
```

---

## ✨ 장점

1. **유연성**: 다양한 캠페인 유형에 대응 가능
2. **사용자 친화적**: 강제가 아닌 권장 사항으로 부담 감소
3. **예외 처리**: 웹서비스, 온라인 이벤트 등 예외 케이스 처리 가능
4. **기본값 유지**: 일반적인 경우(방문체험단)는 여전히 기본 체크됨

---

## 📝 결론

지도 삽입은 이제 **권장 사항**이며, 광고주가 캠페인 특성에 따라 자유롭게 선택할 수 있습니다.
